Make a django package that can be used to turn a normal url into Bell Rock
hidden content.

Django-Bell-Rock

Uses a decorator: @BellRock(secret_key)
requres a POST with a key value pair of "token"="onetimekey"

Remember to prevent multiple requests from the same IP.
